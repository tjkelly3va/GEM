# GEM Edit Log

Chronological record of GEM corpus changes: category renames/retirements, rule recategorizations, and per-session extraction / promotion / consolidation actions. Append-only. Relocated from `gem_rule_categories.md` at Session 255 (where it had been a `## Edit log` section wedged mid-file among the per-policy `### ` sections); entries are preserved verbatim in their migrated order.

- **2026-07-31 / Session 245** — Promoted **NCD 20.33** (Transcatheter Edge-to-Edge Repair (TEER) for Mitral Valve Regurgitation; ncdid=363) from a `planPromote` stub to a full `planDone` extraction: 65 rules, +788 triples. Extracted V3 current text (B1: V1→V2/V3 TEER transition was a reported change). 4 credential mints, 18 concept mints (stroke & TIA reused); 12 new reference stubs (8 transmittals, 2 CRs, 2 NCAs). NCAs CAG-00438N/R linked to the NCD via `referencesPolicy` as policy history (B5, Tom). Source-fidelity typos in standards j/k/l preserved verbatim.
- **2026-06-14 / Session 56** — Retired `credential-definition` from Axis 2 vocabulary. Dropped 3 rules from NCD 210.10 under SKILL.md §Rule Patterns → "What belongs as a rule": R15 (statutory-framing-only cross-reference, corollary #1) and R16/R17 (statutory credential definitions whose content is preserved on `gemi:credentialPrimaryCarePhysician` and `gemi:credentialPrimaryCarePractitioner`, corollary #2 extended to `gem:ProviderCredential`). NCD 210.10 rule count 21 → 18; original R18–R21 renumbered to R15–R18. Rationale: see §4.3(xxii) of the S55 handoff. SKILL.md corollary #2 was extended in the same Generate to name both `gem:ClinicalConcept` and `gem:ProviderCredential`.
- **2026-06-15 / Session 59** — Added L33612 (Suction Pumps) rule table: 31 rules, single-domain (DME), no new axis-2 vocabulary. R3 verbatim-duplicate-suppression noted (captured 1×; appears 3× in source). 8 distinct axis-2 tags used: coverage-scope, credentialed-actor, documentation, eligibility, frequency, non-coverage, service-definition, service-standard.
- **2026-06-15 / Session 60** — Added A52519 (Suction Pumps - Policy Article) rule table: 22 rules, single-domain (DME), no new axis-2 vocabulary. 7 distinct axis-2 tags used: coverage-scope, documentation, non-coverage, service-definition, service-standard, setting-definition, statutory-framing. R2's `setting-definition` tag references the POS 31/32 nursing-facility setting; logged in policy_worklist.json deferred_proposals as the first DMEPOS facility-statutory-denial occurrence of Place-of-Service Codes (Thread 2 of B6).
- **2026-06-16 / Session 62** — Added L36524 (Nerve Conduction Studies and Electromyography) rule table: 41 rules, single-domain (electrodiagnostic testing), no new axis-2 vocabulary. 8 distinct axis-2 tags used: coverage-scope, credentialed-actor, documentation, eligibility, frequency, non-coverage, service-standard, setting-definition (same set as L33612). Reprocessing pass — lcd36524 was first extracted pre-S48 (before rule-description methodology was adopted) and carried zero `gem:ruleDescription` triples; this pass backfilled all 41 at policy level (no scopes — coding content lives on companion article gemi:a54969). Three list-granularity borderlines (B1) resolved Option B (split per item with synthesized/verbatim heading prefix). One duplicate-restatement borderline (B2) resolved (c) verbatim-both, with user-issued project-wide preference signal: "capture verbatim text; downstream resolves differences".
- **2026-06-16 / Session 63** — Added A58075 (Billing and Coding: Hypoglossal Nerve Stimulation for Obstructive Sleep Apnea) rule table: 40 rules across 4 subjects (25 policy-level + 5 each on `groupNewImplant` / `groupRevision` / `groupRemoval`), single-domain policy, no new axis-2 vocabulary. 7 distinct axis-2 tags used: coverage-scope, credentialed-actor, documentation, eligibility, non-coverage, service-definition, service-standard. Reprocessing pass — `gemi:a58075` and its 3 PolicyGroups were first extracted pre-S48 (before rule-description methodology was adopted) and carried zero `gem:ruleDescription` triples; this pass backfilled all 40 on the existing entities (clean scope-backfill — no scope-mint, no description rewrite). Six borderlines resolved: B1=Option B for all 4 lists; B2=(a) capture DNS verbatim on groupNewImplant; B3=(b) split ABN modifier blocks per sentence with `"Modifier <X>:"` prefix (12 modifier rules); B4=(a) capture both NCCI and ABN intro; B5=(a) shared `"no dual diagnosis"` sentence on both Revision and Removal Groups; B6=(a) `"Replacement or revision medically necessary"` rule on Revision Group. Two project-wide skill-refinement candidates registered: verbatim-cross-reference principle (B2) and jigsaw-puzzle-piece principle (B6).
- **2026-06-17 / Session 64** — Added NCD 240.2 (Home Use of Oxygen) rule table: 22 rules, all at policy level (no anchored coding scopes), single-domain policy, no new axis-2 vocabulary. 6 distinct axis-2 tags used: coverage-scope, credentialed-actor, documentation, eligibility, non-coverage, service-standard. Reprocessing pass — `gemi:ncd240.2` was the first policy ever extracted (pre-S48, before the rule-description methodology) and carried zero `gem:ruleDescription` triples at session start; this pass backfilled all 22 on the existing policy entity (clean scope-backfill — no scope-mint, no description rewrite, no clinical-concept mint). Eight borderlines resolved (B1-B8). Two project-wide methodology insights articulated and registered as skill-refinement carry-forwards for future SKILL.md codification: (1) IF/THEN heuristic for granularity decisions; (2) parallel-vs-sequential decision-shape distinction. Worked example for the deferred-proposal "First-class gem:Rule entities and structured decomposition (ζ schema)" produced as `gem_structured_rule_guide.md` (not yet in canonical file set).
- **2026-06-22 / Session 81** — Added A54969 (Billing and Coding: Nerve Conduction Studies and Electromyography - Policy Article) rule table: 42 rows across 3 subjects (3 policy + 27 General + 12 CTS; 0 Chemodenervation), 33 unique source rules (18 General-only + 3 CTS-only + 9 cross-cutting captured 2× per A58075 precedent), single-domain policy, no new axis-2 vocabulary. 10 distinct axis-2 tags used: coverage-scope, credentialed-actor, service-definition, service-standard, documentation, non-coverage, statutory-framing, concurrent-services, frequency, eligibility. B1 = A58075-style Scope column adopted for multi-PolicyGroup articles with deferred Phase 4 Group-scope rules. B2 = `statutory-framing` tag policy formalized: tag applies when statute/regulation/transmittal is syntactic subject of the rule sentence OR explicitly named as the legal framework being applied for a determination; does not apply when citation is supporting authority for a substantive criterion (precedents A52519 R4 + NCD 240.4.1 R1 vs L36524 R35 + A58075 R3).
- **2026-06-22 / Session 82** — Added TN 2476 (Pub. 100-04 transmittal: Screening for STIs and HIBC) rule table: 73 rules, all at policy level (no PolicyGroups), multi-domain (screening/hibc/cross-cutting per NCD 210.10's vocabulary). **First transmittal-policy categorization in project history.** No new axis-2 vocabulary introduced; 11 distinct tags used: coverage-scope, credentialed-actor, concurrent-services, cost-sharing, documentation, eligibility, frequency, non-coverage, procedural, service-definition, setting-definition. `statutory-framing` deliberately not applied (T4: "Effective for DOS" framings and CR/TR cross-citations fail S81 B2's syntactic-subject / framework-being-applied test). Cardinality distribution 22/32/17/2 (singles/doubles/triples/quadruples) yielding 145 `gem:ruleType` tag instances; skews higher-cardinality than corpus average (51/39/10/0.4%) — composite Manual-paragraph rules R70 (§170.3) and R72 (§170.5) are the quadruples. Six tagging borderlines resolved at Plan stage: T1 (TOB lists — 3-way split: positive coverage / negative denial / identification classification), T2 (POS list — `setting-definition` + contextual), T3 (payment-method → `procedural`), T4 (no `statutory-framing` for temporal-modifier or supporting-authority citations), T5 (provider-education publishing → `procedural`), T6 (explanatory "Contractors shall note" → `procedural`). Cycle B (separate Plan→Generate→Verify) migrates all 73 rules to `gemi:tn2476CP_r1` – `gemi:tn2476CP_r73`.
- **2026-06-22 / Session 83** — Added A52514 (Oxygen and Oxygen Equipment - Policy Article) categorization-table section (1 rule on `gemi:a52514_group1`); added Phase 4 anchor-scope sub-table to A52519 (2 rules across `gemi:a52519_group1A` and `gemi:a52519_a7047CodingRule`). **Phase 4 launch** — first migration of anchor-scope `gem:ruleDescription` triples to `gem:PolicyRule` individuals. No new axis-2 vocabulary introduced; tags used for the 3 new PolicyRule individuals: `non-coverage` (×2 — both group-scope normalized 'does not support medical necessity' statements), `coverage-scope` + `procedural` (the anchor-scope coding-correctness directive on `gemi:a52519_a7047CodingRule`). New section-level convention: where a policy has both pre-S83 policy-scope and anchor-scope migrations, the anchor-scope rules go in a `#### Anchor-scope rules` sub-section with its own Scope column (A52519 form); a degenerate single-anchor-scope policy (A52514) gets a flat table with no Scope column. Per-anchor R-numbering restarts each anchor scope per Phase 4 convention (S82 §4.24(d)).
- **2026-06-23 / Session 84** — A58075 Phase 4 migration: 15 anchor-scope `gem:ruleDescription` triples on 3 PolicyGroups (`groupNewImplant`, `groupRevision`, `groupRemoval` — 5 each) migrated to 15 `gem:PolicyRule` individuals at per-anchor R-numbered URIs. Cycle A: second-line migration stamp added under the existing S74 stamp recording the R26–R40 ↔ per-anchor URI mapping. **Inline-leave-as-is form adopted** (S83 Plan-2-A1 alternative to the A52519 sub-section split): the S63 main 40-row Scope-column table already correctly categorizes all 15 anchor-scope rules (R26–R40 with Scope labels); no sub-section needed. Sets precedent for A54969 (same shape: existing Scope-column table from S81). No new axis-2 vocabulary introduced; tag assignments lifted unchanged from S63 (29 `gem:ruleType` triples across the 15 new PolicyRules). No ontology delta (S83 union-class domain extension covers all subsequent Phase 4 work).
- **2026-06-24 / Session 85** — A54969 Phase 4 migration (**Phase 4 completion** — outstanding anchor-scope inventory → 0): 39 anchor-scope `gem:ruleDescription` triples on 2 PolicyGroups (`groupGeneralNCS_EMG` 27 + `groupCTSAutomated` 12; 0 on `groupChemodenervation`) migrated to 39 `gem:PolicyRule` individuals at per-anchor R-numbered URIs (`_r1`–`_r27` / `_r1`–`_r12`). Cycle A: second-line migration stamp under the existing S81 stamp + this entry. **Inline-leave-as-is form** (S84 A58075 precedent): existing 42-row Scope-column table unchanged. **Borderline A1 resolved Option A** — `_rN` follows file/serialization order, not this table's thematic R-order (the table's "Ordering caveat" anticipated the divergence); each PolicyRule's `gem:ruleType` tags were content-matched to its table row and lifted unchanged. 68 `gem:ruleType` triples across the 39 new PolicyRules; no new axis-2 vocabulary. Triple delta +263 (39×5 base + 68 ruleType + 39 hasPolicyRule − 39 ruleDescription; projection matched verified count exactly). No ontology / SHACL / audit-script delta (S83 union-domain covers anchor-scope subjects; both Plan-time gap analyses returned clean).
- **2026-06-25 / Session 90** — Added A52466 (Nebulizers - Policy Article) rule table, **Chunk A** (Article Text): 38 policy-scope rules `gemi:a52466_r1` – `gemi:a52466_r38`. **First direct-to-PolicyRule authoring** (fresh extraction into first-class `gem:PolicyRule` individuals, no intermediate `gem:ruleDescription`-string stage). Single-domain (DME), domain axis omitted. **Two new axis-2 rule_type values introduced and codified as `gem:RuleType` individuals:** `payment-rule` and `coding-correctness` (B1/B2). 7 distinct axis-2 tags used (56 `gem:ruleType` triples): payment-rule (11), non-coverage (18), coding-correctness (15), documentation (8), coverage-scope (2), statutory-framing (1), eligibility (1). Axis-2 list reconciled to the schema (added the 4 already-shipped-but-undocumented S69 values procedural / outcome-measure / study-design / study-quality alongside the 2 new). Five borderlines resolved at Plan: B1 (new `payment-rule`), B2 (new `coding-correctness`; `procedural` confirmed program-administrative, not coding-correctness), B3 (JW/JZ scenarios verbatim Pattern 3), B4 (framing/preamble/pointer dropped), B5 (single-domain). Triple delta +292 (ontology +8: 2 RuleType × 4; instances +284: 38×5 base + 56 ruleType + 38 hasPolicyRule). No SHACL or audit-script change required (both Plan-time gap analyses clean; audit validates ruleType by typing, not enum). Coding Guidelines = Chunk B; anchor-scope rules = Chunk C.
- **2026-06-25 / Session 90 (Chunk B)** — Added A52466 Coding Guidelines rules R39–R80 (42 policy-scope rules): EQUIPMENT (R39–R48), ACCESSORIES (R49–R56), INHALATION DRUGS (R57–R80). No new schema (`service-definition` / `coding-correctness` / `non-coverage` all exist). 3 distinct axis-2 tags (52 `gem:ruleType` triples): service-definition (17), coding-correctness (27), non-coverage (8). Two borderlines: B-B1 (service-definition sentences captured verbatim — concept snippets are truncated, so corollary #2 does not apply), B-B2 (three framing/navigational sentences dropped per B4 precedent). Triple delta +304 (ontology +0; instances +304 = 42×5 base + 52 ruleType + 42 hasPolicyRule). Inter-rule spacing for R1–R80 normalized to blank-line separation. A52466 policy-scope total now 80 rules (R1–R80); anchor-scope rules across the 16 Groups remain Chunk C.
- **2026-06-26 / Session 91 (Chunk C)** — A52466 anchor-scope review across the 16 PolicyGroups: **0 rules** (deliberate-zero). No graph edits — the Coding Information section's substantive content is already modeled (per-Group ICD closed-lists as `gem:exclusivelyCoversCondition`; the DO-NOT 35-HCPCS blanket denial as policy-level `gem:excludesProcedure`, all 35 verified present). The section's only two prose paragraphs are non-rule statements, correctly not captured: the Support Group 1 boilerplate ("…not sufficient by itself to assure coverage. Refer to the LCD section…") restates the coversCondition-unless-denied architecture, and the DO-NOT Group 1 Paragraph is the implicit per-Group complement + the already-modeled blanket denial + the open-world default. Groups 2–16 carry no Group-Paragraph narrative; no per-Group `CODING GUIDELINES:` content blocks exist (the 6125+ `CODING GUIDELINES:` strings are Revision-History sub-headings). Decision (Tom-directed): non-rule statements are not captured and the rule test is unchanged. A52466 rule backfill complete (80 policy-scope; 0 anchor-scope). Handoff §5.2's Chunk C scope (per-Group narratives + Group `CODING GUIDELINES` blocks) rested on a source misread — flagged for the S91 handoff. Bears on `deferred_proposals[42]` (A52466 no longer blocks; full `implemented → complete` promotion still awaits the corpus audit).
- **2026-06-26 / Session 91 (A52519 recat)** — Recategorized `gemi:a52519_a7047CodingRule_r1` (anchor-scope coding directive on `gemi:a52519_a7047CodingRule`): `gem:ruleType` `coverage-scope` + `procedural` → `coding-correctness` (final set: `coding-correctness` alone). Resolves the S90 B2-flagged candidate (handoff §4). Reasoning: `procedural` is program-administrative, not coding-correctness; `coverage-scope` ("what is covered, at what level — the THEN-clause of a positive-coverage rule") mis-fits a negative prohibited-code directive, and the rule's only coverage-boundary aspect is already carried structurally by the anchor's `appliesWhenProcedure A7047` + `excludesProcedure E0485/E0486`. The rule text ("Do not use the oral appliance HCPCS codes E0485 or E0486 for the A7047 oral interface") is a pure prohibited-code directive, so `coding-correctness` is the precise and sufficient tag; `coverage-scope`+`procedural` was S83's two-tag proxy for it. Instances Δ −1 (−`coverage-scope`, −`procedural`, +`coding-correctness`); no schema change (audit validates `ruleType` by typing). Consistency-checked the other 50 `procedural`-tagged rules — ncd160.18 (CED/AHRQ/PI) and the tn2476 contractor-system-edit directives (deliberate S82 T1–T6 calls) are correctly program-administrative; no sibling recats, `procedural` retains 50 valid uses. First task executed under the S91 record-and-proceed borderline process: one settled swap + one confirmed `coverage-scope`-drop borderline.
- **2026-06-26 / Session 92** — Added L33797 (Oxygen and Oxygen Equipment) rule table: 54 rules, all at policy level (no anchored coding scopes), single-domain (home oxygen therapy and oxygen equipment), no new axis-2 vocabulary. Direct-to-`gem:PolicyRule` authoring — fresh mint of `gemi:lcd33797_r1`–`r54` onto the already-extracted `planDone` policy (zero prior rule coverage; 40 coversProcedure / 2 excludesProcedure / 16 modifiers / 56 concepts / 9 credentials / 9 references / Groups I–IV preserved unchanged). 9 distinct axis-2 tags (96 `gem:ruleType` triples): coding-correctness, coverage-scope, credentialed-actor, documentation, eligibility, frequency, non-coverage, payment-rule, service-standard. L33797 operationalizes NCD 240.2; the parent NCD (S64/S77) and L33612 GENERAL boilerplate (S59/S75) served as templates. Borderlines resolved under the S91 record-and-proceed factored decision-tree process (first multi-borderline stress-test): DP-A (drop generic 3-prong test, 0 precedent), DP-B (drop supplier/hospital credential-definitional sentences, capture operative supplier-prohibition R19), DP-C (conjunctive gates one rule each — R2/R28/R35), DP-D (drop overnight-oximetry restatement), DP-E (drop definitions on existing concepts), DP-F (OSA treated+unmasked as 2 rules), DP-G (liter-flow one rule R42), DP-H (exercise protocol R23 + recovery R24), DP-I (drop Associated-Info documentation block). Triple delta +420 (instances +420 = 54×5 base + 96 ruleType + 54 hasPolicyRule; ontology +0). No SHACL or audit-script change (no ontology-semantics change; both Plan-time gap analyses clean).
- **2026-06-26 / Session 93** — Added L33800 (Respiratory Assist Devices) rule table: 40 rules, all at policy level (no anchored coding scopes), single-domain (RAD bi-level PAP therapy), no new axis-2 vocabulary. Direct-to-`gem:PolicyRule` authoring — fresh mint of `gemi:lcd33800_r1`–`r40` onto the already-extracted `planDone` policy (zero prior rule coverage; 22 coversProcedure / 3 excludesProcedure / 4 modifiers / 70 concepts / 4 credentials / 7 references preserved unchanged). 9 distinct axis-2 tags (81 `gem:ruleType` triples): coding-correctness, coverage-scope, credentialed-actor, documentation, eligibility, frequency, non-coverage, payment-rule, service-standard. L33800 is the RAD sister LCD to L33797 (oxygen, S92); L33797 and L33612 (DMEPOS GENERAL + post-FR-1780-F REFILL boilerplate, S59/S75) served as templates; `ncd240.9` (CRF-COPD parent) is `planDone` with zero rules so R9/R18 are verbatim cross-references (corollary #4). Borderlines resolved under the S91 record-and-proceed factored decision-tree process (confirmed in one batch): DP1 (drop CILMN three-prong + §1862 framing + navigational; no synthesized master R1 — R2 is the operative covered-disorder rule), DP2 (drop DEFINITIONS pure definitions preserved on concepts; keep operative R3 AHI/CAHI-<2hr + R4 discontinuation), DP3 (conjunctive named-disorder gates one rule each — R5/R6/R7/R8), DP4 (drop Ventilators framing; capture R10–R13), DP-FSS (FSS payment+denial one cohesive rule R12; L33797 R43 precedent), DP-accessories (max-amount table one cohesive rule R19; per-code split deferred to structured frequency triples), DP-MISC (capture R40 retention rule), plus drops of Sleep-Tests para-1, Replacement scope sentence, Coding-Info Group-1 disclaimer + bare modifier definitions, and the Associated-Info documentation block (= L33797 DP-I). GENERAL (R21–R25) + REFILL (R26–R32) follow L33612 per-sentence verbatim-precedent. Triple delta +321 (instances +321 = 40×5 base + 81 ruleType + 40 hasPolicyRule; ontology +0). No SHACL or audit-script change (no ontology-semantics change; both Plan-time gap analyses clean).
- **2026-06-26 / Session 94** — Added L33718 (Positive Airway Pressure (PAP) Devices for the Treatment of Obstructive Sleep Apnea) rule table: 42 rules, all at policy level (no anchored coding scopes), single-domain (PAP/CPAP/bi-level for OSA), no new axis-2 vocabulary. Direct-to-`gem:PolicyRule` authoring — fresh mint of `gemi:lcd33718_r1`–`r42` onto the already-extracted `planDone` policy (zero prior rule coverage; 21 coversProcedure / 1 excludesProcedure / 4 modifiers / 53 concepts / 4 credentials / 9 references preserved unchanged). 8 distinct axis-2 tags (94 `gem:ruleType` triples): coverage-scope, credentialed-actor, documentation, eligibility, frequency, non-coverage, payment-rule, service-standard (no coding-correctness — L33718 has no ventilator-miscoding rule). L33718 is the OSA/PAP sister LCD to L33800 (RAD, S93) and L33797 (oxygen, S92); L33800 and L33612 (DMEPOS GENERAL/REFILL boilerplate, S59/S75) served as templates; companion PA A52467 carries the ICD-10 lists (relocated in R6); the concurrent-oxygen section cross-references `lcd33797`. Borderlines resolved under the S91 record-and-proceed factored decision-tree process (confirmed in one batch): D1 (drop CILMN three-prong + §1862 framing + navigational; no synthesized master — R2/R3 are the operative E0601/E0470 gates), D2 (drop DEFINITIONS pure definitions preserved on concepts incl. the 'Sleep time can only be measured in Type I/II' sub-clause; keep operative R1 AHI/RDI-<2hr), D3 (drop 'PAP device' term-scope definition — redundant with inline parentheticals), D4 (drop non-OSA-use scope-exclusion pointer to RAD LCD), G1 (R10 clinical-benefit / R11 adherence-threshold kept as 2 rules), G2 (R12 denial complement separate), G3 (R22 all-oxygen-reqs + stationary-only as one cohesive rule), G4 (R26 entering-Medicare denial folded into the gate), G5 (R27 carve-out separate), G6 (R15 substitution trial-timing >30/<30 fork as one rule), G7/DP-accessories (R29 max-amount table one cohesive rule, per-code split deferred to structured frequency triples), D5 (drop §1833(e) Associated-Info block + GENERAL-DOC-REQ list + POLICY-SPECIFIC pointer — no L33800-R40-equivalent retention rule in L33718), D6 (drop Epworth Sleepiness Scale appendix), D7 (drop Coding-Info Group-1 disclaimer + EY/GA/GZ/KX modifier definitions — preserved on modifier individuals). GENERAL (R30–R34) + REFILL (R35–R41) follow L33800/L33612 per-sentence verbatim-precedent; two verbatim differences from L33800: R36 carries the inline 17-HCPCS refill code list, and R42 humidifier lacks 'paid separately' (→ no payment-rule, unlike L33800 R20). Triple delta +346 (instances +346 = 42×5 base + 94 ruleType + 42 hasPolicyRule; ontology +0). No SHACL or audit-script change (no ontology-semantics change; both Plan-time gap analyses clean). policy_worklist.json unchanged (already current; rule backfill does not alter status/counts).
- **2026-06-27 / Session 96** — Added A52492 (Tracheostomy Care Supplies - Policy Article) rule table: 21 policy-scope rules `gemi:a52492_r1` – `r21`, all at policy level (no anchored coding scopes), single-domain, no new axis-2 vocabulary. Direct-to-`gem:PolicyRule` authoring onto the already-extracted `planDone` policy (zero prior rule coverage; 18 coversProcedure / 3 excludesProcedure / 10 ICD `exclusivelyCoversCondition` / 1 modifier / 2 credentials / 2 references / 30 concepts preserved unchanged). 9 distinct axis-2 tags (34 `gem:ruleType` triples): coding-correctness (11), documentation (6), non-coverage (5), coverage-scope (4), service-definition (3), payment-rule (2), eligibility (1), statutory-framing (1), concurrent-services (1). Closest precedent A52466 (Nebulizers Article, S90); same Final-Rule-1713 boilerplate (R5–R9 = A52466 R21–R25, with R9 omitting "prior to delivery" per A52492 source). Borderlines resolved under the S91 record-and-proceed factored decision-tree process (confirmed in one batch): B1 (drop coverage-preamble in full — generic three-prong + §1862 + Prosthetic-Benefit; the R&N-in-LCD precondition already structural via `referencesPolicy lcd33832`), B2 (descriptor-definition cluster — corollary #2 DROP of the 9 descriptors whose full verbatim is on concepts, the deliberate A52466 asymmetry; B-B1 capture of A4481/A7504 whose verbatim is on no concept; directive-bearing A4483/A7526/all-inclusive-tubes captured for their directives), B3 (AU-modifier 3 rules — R4/R14 restatement pair per corollary #3 + R15 exclusivity), B4 (Column I/II one Pattern-3 rule with linearized pairs; instantiates deferred #45, R12/R13 instantiate #46). Triple delta +160 (instances +160 = 21×5 base + 34 ruleType + 21 hasPolicyRule; ontology +0). No SHACL or audit-script change (no ontology-semantics change; both Plan-time gap analyses clean). policy_worklist.json unchanged (a52492 already processed/in-graph; refs in-graph; #45/#46 already `first_seen_in` A52492; rule backfill does not alter status/counts).
- **2026-06-27 / Session 97** — Added A52517 (Respiratory Assist Devices - Policy Article) rule table: **24** policy-scope rules `gemi:a52517_r1` – `gemi:a52517_r24`. Direct-to-PolicyRule authoring on the already-extracted `planDone` policy (zero prior rule coverage). Single-domain (DME). No new axis-2 vocabulary. 8 distinct axis-2 tags (37 `gem:ruleType` triples): coding-correctness (12), non-coverage (9), documentation (8), payment-rule (2), coverage-scope (2), service-definition (2), credentialed-actor (1), statutory-framing (1). Five Plan borderlines confirmed (DP1–DP5). **Two same-session corrections:** (a) the "A9279 statutorily non-covered" NMN sentence, overlooked in the initial 25-rule pass, added (now R24); (b) the A4604/A7027 pure-definition service-definition rules (initial R17/R20) dropped under the **extended corollary #2** (HCPCS descriptor co-resident — see methodology entry below) and the set renumbered to a contiguous R1–R24. Final triple delta +181 (instances only: 24×5 base + 37 ruleType + 24 hasPolicyRule). Preserved baseline unchanged (7 covers / 3 excludes / 3 modifiers / 5 credentials / 42 concepts / 3 references); mass conservation strict line-subsequence; all 24 ruleDescriptions verbatim-fidelity-checked.
- **2026-06-27 / Session 97** — Added A52467 (Positive Airway Pressure (PAP) Devices for OSA - Policy Article) rule table: 41 policy-scope rules `gemi:a52467_r1` – `gemi:a52467_r41`. Direct-to-PolicyRule authoring on the already-extracted `planDone` policy. Single-domain (DME); the rule-rich DME article (deep documentation section: initial-evaluation elements, E0601→E0470 switch criteria, KX multi-month framework, beneficiaries-entering-Medicare, concurrent-oxygen). No new axis-2 vocabulary. 9 distinct axis-2 tags (56 `gem:ruleType` triples): coding-correctness (21), documentation (18), non-coverage (7), service-definition (3), payment-rule (2), coverage-scope (2), credentialed-actor (1), statutory-framing (1), concurrent-services (1). Applied the **extended corollary #2**: dropped E0601/E0470/E0471/A4604 pure device definitions (identity on the co-resident HCPCS descriptor); kept descriptor sentences with a rule-bearing increment (A7032 R34, A7033 R35, A7027 R36, auto-titrating R33). R4 captures the A9279 statutory non-coverage (the rule A52517 had missed). R9 includes "prior to delivery" (= A52466 R25 form). ICD Group paragraphs dropped (= A52466 Chunk C). Triple delta +302 (instances only: 41×5 base + 56 ruleType + 41 hasPolicyRule). No SHACL or audit-script change. Preserved baseline unchanged (8 covers / 3 excludes / 1 exclusivelyCoversCondition / 4 modifiers / 5 credentials / 82 concepts / 7 references); mass conservation strict line-subsequence; all 41 ruleDescriptions verbatim-fidelity-checked.
- **2026-06-27 / Session 97 (methodology)** — Extended **corollary #2** (SKILL.md §Rule Patterns) to name the external `hcpcs:` (and `icd10:`/`cpt:`) code IRI as a definition-bearing named entity: the HCPCS dataset (`skos:prefLabel` official descriptor per code) always sits co-resident in the knowledge graph (Tom, S97), so a pure HCPCS device/item definition is dropped — the code's identity is on its descriptor — and only the rule-bearing increment beyond the descriptor (coding directive, unit-of-service logic, prohibition, code-selection distinction) is captured. The co-resident HCPCS dataset is **not** added to the 12-file canonical set or audit `TTL_FILES`. **Corpus-review flag:** prior B-B1 service-definition captures under the "full verbatim lives nowhere else" rationale — A52466 (~17), A52492 (R17 A4481 / R19 A7504) — predate this extension and overlooked the co-resident descriptor; logged in policy_worklist.json for a checkpoint review. A52517 (R17 A4604 / R20 A7027) corrected same-session.
- **2026-06-27 / Session 98** — **HCPCS-descriptor corollary #2 corpus review** (Tom-directed) of the prior B-B1 `service-definition` captures flagged in S97, run against the co-resident `HCPCS.ttl` `skos:prefLabel` descriptors. **A52466:** 9 pure device-identity definitions dropped — E0565 (old R40), E0570 (R41), E0572 (R42), E0574 (R43), E0575 (R47), K0730 (R48), A7012 (R51), A7016 (R52), A4218 (R77); survivors renumbered contiguous **R1–R71** (was R1–R80). **A52492:** 1 dropped — A7504 (old R19, "filter that fits into A7503", redundant with the `hcpcs:` descriptor); survivors renumbered contiguous **R1–R20** (was R1–R21). **6 kept for a functional increment beyond the descriptor:** A52466 R43/R44 (unit-of-service composition + code-selection), R45/R48 (coding directives), A52492 R17 ("It is not an HME" code-selection), R18 ("should not be billed as an HME" prohibition). **4 concept-only `service-definition` rules left untouched this pass** (no single HCPCS code, outside the descriptor test): A52466 R39 (two-device framing) and R49/R54/R55 (compounded / unit-dose / concentrated-form definitions) — flagged for a possible concept-path follow-up (their `gem:ClinicalConcept` descriptions currently carry only truncated provenance snippets). Triple delta **−70** (instances only: 10 rules × [5 base + 1 `gem:ruleType` + 1 `gem:hasPolicyRule`]); `serviceDefinition` `gem:ruleType` 66 → 56; PolicyRule total 693 → 683; instances 19,597 → 19,527. Instances-file-only graph change (no ontology/SHACL edit). Verification: parse round-trips; content-based mass conservation (ruleDescription multiset = original − exactly the 10 dropped; ruleType multiset = original − 10 `serviceDefinition`, all others unchanged); both policies contiguous with matching `hasPolicyRule` link lists; all 683 PolicyRules well-formed; authoritative audit **0 SHACL/structural** (only the expected modified-file `hash_verify` mismatches). `policy_worklist.json` corpus-review `deferred_proposals` entry advanced to `resolved`.
- **2026-06-27 / Session 99** — **Concept-path corollary #2 review** of A52466's 4 remaining concept-only `service-definition` rules (the S98-deferred follow-up, `deferred_proposals[83]`), governed by the *original* `gem:ClinicalConcept` full-verbatim corollary #2 (not the S97 HCPCS-descriptor extension). **3 dropped** after first extending each rule's intended concept home to the full verbatim definitional sentence (replacing the truncated `…`-snippet): old R49 → `conceptCompoundedInhalationSolution`, old R54 → `conceptUnitDoseForm`, old R55 → `conceptConcentratedForm`. **R39 retained** (two-device nebulization framing): no single concept is its definition home — its three sentences distribute across `conceptNebulization` (s1) / `conceptPneumaticCompressorNebulizer` (s2) / `conceptUltrasonicOrElectronicNebulizer` (s3), and the "exactly two types, these two" partition is emergent, not a discrete fact on any one concept; corollary #3 verbatim-capture posture favors keeping. Survivors renumbered contiguous **R1–R68** (was R1–R71). Triple delta **−21** (instances only: 3 rules × [5 base + 1 `gem:ruleType` + 1 `gem:hasPolicyRule`]); the 3 `gem:description` extensions are object-literal edits (net 0 triples). `serviceDefinition` `gem:ruleType` 56 → 53; PolicyRule total 683 → 680; instances 19,527 → 19,506. Instances-file-only graph change (no ontology/SHACL edit). Verification: parse round-trips; content-based mass conservation (ruleDescription multiset = original − exactly the 3 dropped, 0 added; ruleType multiset = original − 3 `serviceDefinition`, all 20 others unchanged); A52466 contiguous R1–R68 with matching `hasPolicyRule` link list; all 680 PolicyRules well-formed; the 3 extended concept descriptions carry full verbatim with no residual ellipsis; authoritative audit **0 SHACL/structural** (only the expected modified-file `hash_verify` mismatch). `policy_worklist.json` concept-path `deferred_proposals[83]` advanced to `resolved`. Closes A52466's rule backfill (68 policy-scope rules; descriptor-test and concept-path reviews both complete).

- **2026-06-28 / Session 100** — Added A52494 (High Frequency Chest Wall Oscillation Devices - Policy Article) rule categorization: 13 policy-scope rules `gemi:a52494_r1`–`_r13` minted on the already-extracted `planDone` policy (zero prior rule coverage). No new axis-2 values (documentation, coding-correctness, non-coverage, coverage-scope, statutory-framing). DP-D — structural `gem:PolicyCodingRule` reification of R13 and the broader rule-form complement (structured ⟷ NL) architecture — deferred to the 40-policy checkpoint as `deferred_proposals[84]`. Instances-file banner discipline: deprecated `# Extracted:` provenance line removed from the A52494 block.

- **2026-06-28 / Session 100** — Added A52510 (Mechanical In-exsufflation Devices - Policy Article) rule categorization: 8 policy-scope rules `gemi:a52510_r1`–`_r8` minted on the already-extracted `planDone` policy (zero prior rule coverage). No new axis-2 values. Device-definition sentence dropped under corollary #2 (full verbatim already on `gemi:conceptMechanicalInExsufflationDevices`). A52510 appended to `deferred_proposals[84].also_seen_in` (carries a reified `gem:PolicyGroup` → complement-backfill candidate). Instances-file banner discipline: deprecated `# Extracted:` line removed from the A52510 block.

- **2026-06-28 / Session 100** — Added A52495 (Intrapulmonary Percussive Ventilation System - Policy Article) rule categorization: 6 policy-scope rules `gemi:a52495_r1`–`_r6` minted on the already-extracted `planDone` policy (zero prior rule coverage). No new axis-2 values. IPV device-definition dropped under corollary #2 (full verbatim on `gemi:conceptIntrapulmonaryPercussiveVentilationSystem`). No ICD table / no `gem:PolicyGroup` → not a complement-backfill candidate. Instances-file banner discipline: deprecated `# Extracted:` line removed from the A52495 block. Completes the S100 moderate-tier backfill (A52494, A52510, A52495).
- **2026-06-28 / Session 101** — Began NCD 280.1 (Durable Medical Equipment Reference List - NCD) rule backfill, **Pass 1 of 4 (items A–C)**: 20 policy-scope rules `gemi:ncd280.1_r1`–`_r20` minted on the already-extracted `planDone` policy (zero prior rule coverage; 132 refersToClinicalConcept / 3 credentials / 30 references preserved unchanged). Narrow extraction (Tom-confirmed S101): one `gem:PolicyRule` per determination-bearing row (Covered-if / Deny-with-reason); the 25 pure cross-reference "(See X)" rows get no rule (guidance preserved via concept `gem:description` + policy-level `gem:referencesPolicy`). No new axis-2 vocabulary. 6 distinct axis-2 tags (40 `gem:ruleType` triples): non-coverage (13), statutory-framing (11), coverage-scope (7), eligibility (7), credentialed-actor (1), frequency (1). **Verbatim sourced from the PDF, not concept strings** (concept `Governing text` not uniformly exact — R10 restores the §1861(n) cite dropped by `conceptBedLounge`; R2 keeps source typo "§1861 (n)"; R17 keeps "Manual , Chapter"); all 20 byte-exact substring-checked against the footer-stripped, column-reconstructed source. Triple delta +160 (instances only: 20×5 base + 40 ruleType + 20 hasPolicyRule; ontology +0). Mass conservation: 0 baseline triples removed. No SHACL or audit-script change. policy_worklist.json: `deferred_proposals[85]` (determination+cross-reference pattern) and `[86]` (concept §-cite-drop cleanup, D6) logged.
- **2026-06-28 / Session 101** — NCD 280.1 rule backfill **Pass 2 of 4 (items D–L)**: 26 policy-scope rules `gemi:ncd280.1_r21`–`_r46` minted on the `planDone` policy. Narrow extraction (Infusion Pumps row skipped as a pure cross-reference). No new axis-2 vocabulary. 5 distinct axis-2 tags (49 `gem:ruleType` triples): non-coverage (21), statutory-framing (18), coverage-scope (5), eligibility (3), credentialed-actor (2). R33 Face Masks (oxygen) verbatim sourced from PDF (shared `conceptOxygenMask`). Fidelity preserved: R36 double §-cite, R42 source typo "supply;pressure", R38/R39 mixed straight/curly apostrophes ("MAC's" / "patient's"); all 26 byte-exact substring-checked. Triple delta +205 (instances only: 26×5 base + 49 ruleType + 26 hasPolicyRule; ontology +0). Mass conservation: 0 baseline triples removed. No SHACL or audit-script change. NCD 280.1 running total: 46 rules (R1–R46), 2 of 4 passes complete.
- **2026-06-28 / Session 101** — NCD 280.1 rule backfill **Pass 3 of 4 (items M–P)**: 24 policy-scope rules `gemi:ncd280.1_r47`–`_r70` minted on the `planDone` policy. Includes the D2 Portable Oxygen Systems split (R62 Regulated→Covered / R63 Preset→Deny, source-garbled verbatim captured per corollary #3, verified against the rendered page image) and the D4 Oscillating Beds duplicate (R54). Shared oxygen-family concepts (R56 Oxygen, R57 Oxygen Humidifiers) sourced from the PDF. Pure-pointer rows skipped (Oxygen Regulators (Medical), Oxygen Tents, Portable Paraffin alias). No new axis-2 vocabulary. 6 distinct axis-2 tags (42 `gem:ruleType` triples): coverage-scope (13), non-coverage (11), eligibility (8), statutory-framing (5), credentialed-actor (4), payment-rule (1). All 24 byte-exact substring-checked. Triple delta +186 (instances only: 24×5 base + 42 ruleType + 24 hasPolicyRule; ontology +0). Mass conservation: 0 baseline triples removed. No SHACL or audit-script change. NCD 280.1 running total: 70 rules (R1–R70), 3 of 4 passes complete.
- **2026-06-28 / Session 101** — NCD 280.1 rule backfill **Pass 4 of 4 (items Q–W)** — **policy complete**: 35 policy-scope rules `gemi:ncd280.1_r71`–`_r105` minted on the `planDone` policy. Includes the D2 Rolling Chairs split (R73 Covered / R74 Denied). Two source-garbled verbatims captured per corollary #3 (image-verified): R73 "rollabout" (byte-exact to text layer; soft-hyphen dropped) and R103 Whirlpool Bath "(standard)condition". Hybrid Deny+pointer rows R83/R87/R104 (statutory) vs R105 White Canes (non-coverage only, §280.2 cross-ref). No new axis-2 vocabulary. 5 distinct axis-2 tags (69 `gem:ruleType` triples): coverage-scope (21), eligibility (17), non-coverage (14), statutory-framing (11), credentialed-actor (6). All 35 byte-exact verified (31 clean, R73/R103 image-verified, R79/R80/R82 subsequence-confirmed through col-bleed). Triple delta +279 (instances only: 35×5 base + 69 ruleType + 35 hasPolicyRule; ontology +0). Mass conservation: 0 baseline triples removed. No SHACL or audit-script change. **NCD 280.1 rule backfill complete: 105 rules (R1–R105), all 4 passes (A–W) done.**
- **2026-06-29 / Session 102** — NCD 240.9 (Noninvasive Positive Pressure Ventilation (NIPPV) in the Home for CRF consequent to COPD - NCD) rule backfill, **complete**: 25 policy-scope rules `gemi:ncd240.9_r1`–`_r25` minted on the already-extracted `planDone` policy (49 `refersToClinicalConcept` / 2 credentials / 6 references preserved unchanged). Generated in two passes (Pass 1 RADs R1–R13, +103 triples; Pass 2 HMVs + §D R14–R25, +96 triples). Medium granularity (Tom-confirmed S102): one rule per required criterion in the "all the following criteria are met" blocks; δ-prefix scaffold on the 14 criteria/eval sub-items. No new axis-2 vocabulary. 7 distinct axis-2 tags (49 `gem:ruleType` triples): eligibility (14), credentialed-actor (10), coverage-scope (9), frequency (8), documentation (6), service-standard (1), statutory-framing (1). Borderlines B1 (RAD first-eval = 1 rule), B2 (§D conforming-changes-to-280.1 dropped — cross-ref via `referencesPolicy ncd280.1`), B3 (`statutory-framing` on R25 MAC §1862(a)(1)(A)), B4 (HMV §II.(a) umbrella kept, R14), B5 (R15 "(i)" verbatim), B6 (no `setting-definition` per 240.2 R1), B7 (eval rule-type convention). Two verbatim source artifacts preserved per corollary #3 (PDF authoritative): R18 "if the patient or cannot be otherwise alerted" (apparent dropped words), R24 "an or onasal mask" / "(e.g.,mouthpiece" — confirmed in raw extraction, surfaced to Tom. Verbatim sourced from the PDF (footer-stripped, soft-wrap de-hyphenated: "6-month", "2-week"); all 25 round-trip byte-exact from the graph. Triple delta +199 (instances only: 25×5 base + 49 ruleType + 25 hasPolicyRule; ontology +0). Mass conservation: 0 baseline triples removed. No SHACL or audit-script change. Contrast `tn48` (resolved earlier this session as a no-rules summarizing transmittal): NCD 240.9 is a self-contained NCD whose coverage logic is its own. Remaining concept-only rule-backfill targets: ncd240.2.1, ncd310.1.

- **2026-07-01 / Session 105** — Added NCD 240.2.1 (Home Use of Oxygen in Approved Clinical Trials) rule backfill: 2 policy-scope rules `gemi:ncd240.2.1_r1`–`_r2` minted on the already-extracted `planDone` policy (zero prior rule coverage; 25 `gem:ClinicalConcept` / 5 `gem:referencesPolicy` preserved unchanged). No new axis-2 vocabulary. 2 distinct tags (3 `gem:ruleType` triples): coverage-scope (2), eligibility (1). R1 = single-sentence §B covered-indication (arterial PO2 56–65 mmHg or oxygen saturation at or above 89%, plus enrollment in a CMS-approved, NHLBI-sponsored clinical trial; effective-date phrase kept verbatim). R2 = §D "does not alter" non-alteration clause — first capture under the new § "Scope-boundary sentences: capture vs. drop". Triple delta +15 (instances only: 2×5 base + 3 ruleType + 2 hasPolicyRule; ontology +0). No SHACL or audit-script change. `policy_worklist.json` unchanged; `policies_processed` stays 33 (backfill on an already-processed policy).

- **2026-07-01 / Session 105** — Added NCD 310.1 (Routine Costs in Clinical Trials) rule backfill: 33 policy-scope rules `gemi:ncd310.1_r1`–`_r33` minted on the already-extracted `planDone` policy (zero prior rule coverage; 72 `gem:refersToBenefitCategory` / 13 `gem:ProviderCredential` / 23 `gem:ClinicalConcept` / 5 `gem:referencesPolicy` preserved unchanged). No new axis-2 vocabulary. 10 distinct tags (56 `gem:ruleType` triples): coverage-scope (12), eligibility (12), study-design (9), non-coverage (6), procedural (6), service-definition (4), study-quality (2), statutory-framing (2), documentation (2), payment-rule (1). Enumerated lists atomized one-rule-per-item with a δ-prefix section scope (NCD 160.18 / 240.9 precedent): 3 routine-cost exclusions, 3 inclusions, 3 §A requirements, 7 desirable characteristics, 4 auto-qualified categories. R9 = "does not withdraw" scope-boundary capture. Full Section-B qualification machinery captured as `procedural` rules (BL-1, Tom-confirmed; TN 2476 precedent). Effective-date phrases kept verbatim per provision (7/9/2007; 9/19/2000). Verbatim fidelity: curly quotes on R23 ("panel") / R30 ("reasonable and necessary"); § / §§ on R23 / R32; em-dash on R8; R2 drops trailing "except:", R9 drops trailing navigational sentence. Triple delta +254 (instances only: 33×5 base + 56 ruleType + 33 hasPolicyRule; ontology +0). No SHACL or audit-script change. `policy_worklist.json` unchanged; `policies_processed` stays 33 (backfill on an already-processed policy). **Concept-only rule-backfill campaign complete** (ncd240.2.1 + ncd310.1 the last targets; ncd240.9 already carried rules).

- **2026-07-01 / Session 105** — Added § "Scope-boundary sentences: capture vs. drop" to the Axis-2 categorization guidance: a policy-scope non-alteration/non-withdrawal clause is captured as a single `coverage-scope` rule (verbatim, cross-ref preserved per SKILL.md corollary #4), distinct from a bare navigational scope-boundary pointer, which is dropped (generalizes the L33718 D4 / S94 per-policy precedent). Precedents: `gemi:ncd240.2.1_r2` ("does not alter"), `gemi:ncd310.1_r9` ("does not withdraw"). Cross-ref added to SKILL.md §Rule Patterns → "What belongs as a rule" (corollary #4 parenthetical). No axis-2 vocabulary or schema change.

- **2026-07-01 / Session 107** — **Promoted NCD 20.29 (Hyperbaric Oxygen Therapy)** from a `planPromote` TN 48 stub to a fully-extracted `planDone` policy in its own delimited section (first promotion in the TN 48 stub-promotion campaign; 13 stubs remain). 7 policy-scope rules `gemi:ncd20.29_r1`–`_r7`; 61 clinical-concept links (59 reuse + 2 mint: `conceptChronicNonHealingWound`, `conceptSmokeInhalationWithPulmonaryInsufficiency`); 3 benefit categories (reuse); 5 policy references (tn203 [+ reciprocal `revisesPolicy`], tn3921, cag00060N, cag00060R, ncd270.5); 4 new reference stubs minted (tn203, tn3921, cag00060N, cag00060R). No new axis-2 vocabulary; 6 distinct tags (12 `gem:ruleType` triples): coverage-scope (5), non-coverage (2), frequency (2), eligibility (1), setting-definition (1), documentation (1). Corollary #2 dropped 3 definition sentences (HBO / standard-wound-care / failure-to-respond) carried verbatim on concepts (candidate 12 → 7 rules). Instances triple delta **+172** (policy 88 + rules 47 + concepts 10 + stubs 25 + workflow-state +10 − stub −6 − workflow-state −2; 21,058 → 21,230); mass accounting verified, all post-flight assertions passed. **`gem_audit.py` extended:** `check_proposal_b` gains **Category E (superseded-revision)** — a `revisesPolicy` link whose target carries a *different, reciprocated* current transmittal is not a Category D gap — with self-test **Variant 36** (36/36 pass); resolves the tn48↔ncd20.29 bidirectional-linking gap (tn48 is Revision-History, superseded by tn203; Tom-confirmed exclude-reciprocal, per the three-contexts rule; a lone unreciprocated `revisesPolicy` with no current transmittal still returns Category D RED, so genuine gaps like S50 TN 78 are not masked). SKILL.md: §Policy-to-Policy superseded-revision note added; empirical counts marker → **as of S107, 29/0/168**. `policy_worklist.json`: `policies_processed` 33 → 34, `last_updated` 2026-07-01. TTL header processed-policy list → 34 (NCD 20.29 appended).

- **2026-07-03 / Session 109** — **Promoted NCD 160.7.1 (Assessing Patient's Suitability for Electrical Nerve Stimulation Therapy)** from a `planPromote` TN 48 stub to a fully-extracted `planDone` policy in its own delimited section (second promotion in the TN 48 stub-promotion campaign; 12 stubs remain). 15 policy-scope rules `gemi:ncd160.7.1_r1`–`_r15`; 13 clinical-concept links (all reuse — TN 48 pre-seeded), 2 provider credentials (reuse), 4 benefit categories (reuse), 4 policy references (ncd160.13, ncd160.7, ncd160.27, tn48 [reciprocal to the pre-existing `tn48 revisesPolicy ncd160.7.1`]); 1 new reference stub minted (`gemi:ncd160.27`, TENS for CLBP, queued as the next promotion). No new axis-2 vocabulary; 7 distinct tags (15 `gem:ruleType` triples): coverage-scope (5), frequency (2), documentation (2), non-coverage (2), payment-rule (2), setting-definition (1), credentialed-actor (1). **Current-transmittal reciprocity** (contrast NCD 20.29): TN 48 is the current Transmittal Information field entry; TN 78 / TN 33 / TN 26 / CR 4278 are Revision-History-only and excluded. No codes (complete PDF, no gate); §1862(a)(1) captured in-rule (R15). `publicationNumber "100-3"` (PDF verbatim, Tom-directed) and full `dc:source` URL (`…NCDId=63&NCDver=2`, Tom-directed). Global counts: `planDone` 34 → 35, `referencesPolicy` 168 → 172, `gem:PolicyRule` 879 → 894, `revisesPolicy` unchanged (29). `policy_worklist.json`: `policies_processed` 34 → 35, `last_updated` 2026-07-03. TTL header processed-policy list → 35. SKILL.md empirical-counts marker → 29/0/172. No SHACL / audit-script / schema change.

- **2026-07-03 / Session 110** — **Promoted NCD 160.27 (Transcutaneous Electrical Nerve Stimulation (TENS) for Chronic Low Back Pain (CLBP))** from a `planPromote` reference stub to a fully-extracted `planDone` policy in its own delimited section (third promotion in the TN 48 stub-promotion campaign; `ncd160.27` was a fresh reference stub minted at S109, not one of the 12 remaining campaign stubs, which are unchanged). First **CED** policy promoted in the campaign. 26 policy-scope rules `gemi:ncd160.27_r1`–`_r26`; 7 clinical-concept links (5 reuse + 2 mint: `conceptChronicLowBackPain`, `conceptLowBackPain`); 1 benefit category (DME, reuse); 0 provider credentials; 8 policy references (ncd160.13, ncd160.7.1, ncd280.13 reuse; ncd10.2, tn149, tn2511, tn2605, cag00429N new stubs); 5 new reference stubs minted (ncd10.2, tn149, tn2511, tn2605, cag00429N). Reciprocals: `tn149 revisesPolicy ncd160.27` (current Transmittal Information field); `tn2605 revisesPolicy tn2511` (claims-processing successor lineage). No new axis-2 vocabulary; 8 distinct tags (33 `gem:ruleType` triples): study-quality (14), study-design (5), coverage-scope (3), eligibility (3), outcome-measure (3), definition (2), statutory-framing (2), non-coverage (1). Section-B CED machinery captured with the δ-prefix scaffold + study-* types (NCD 160.18 / 310.1 precedent); a–m Standards of Scientific Integrity via the direct NCD 160.18 template. Revision-history exclusion: TN 144 / CR 7836 and the ICD-conversion CRs excluded; TN 149 + TN 2511/2605 captured. Corollary #2 dropped the Item/Service Description paragraph and the §D §280.13-incorporation note (captured via `referencesPolicy ncd280.13`). `publicationNumber "100-3"` (PDF verbatim, Tom-directed); `dc:source` full URLs (Tom-directed). Global counts: `planDone` 35 → 36, `planPromote` 92 → 96, `gem:PolicyRule` 894 → 920, `referencesPolicy` 172 → 180, `revisesPolicy` 29 → 31, `gem:ClinicalConcept` 1294 → 1296, `gem:NCDpolicy` 57 → 58. `policy_worklist.json`: `policies_processed` 35 → 36, `last_updated` 2026-07-03 (no new worklist entries — all references minted as stubs). TTL header processed-policy list → 36. SKILL.md empirical-counts marker → as of S110, 31/0/180. No SHACL / audit-script / schema change.

- **2026-07-03 / Session 111** — **Promoted NCD 40.2 (Home Blood Glucose Monitors)** from a `planPromote` TN 48 stub to a fully-extracted `planDone` policy in its own delimited section (fourth promotion in the TN 48 stub-promotion campaign; 11 campaign stubs remain). 11 policy-scope rules `gemi:ncd40.2_r1`–`_r11`; 14 clinical-concept links (12 reuse [TN 48 / NCD 280.1 pre-seeded] + 2 mint: `conceptLancet`, `conceptGlucoseConcentration`); 1 provider credential (mint: `credentialPatientsPhysician`, prefLabel "patient's physician"); 1 benefit category (DME, reuse); 2 policy references (cag00161N new NCA stub + tn48 reciprocal to the pre-existing `tn48 revisesPolicy ncd40.2`); 1 new reference stub minted (`cag00161N`, NCAId=35, web-resolved). No new axis-2 vocabulary; 6 distinct tags (15 `gem:ruleType` triples): coverage-scope (4), eligibility (5), documentation (2), credentialed-actor (2), non-coverage (1), payment-rule (1). Two coverage-gate umbrellas kept (R4, R8); enumerated conditions δ-prefixed (R5–R7, R9–R10). Revision-history exclusion: TN 163/CR 2445, TN 75, TN 27, CR 4278; the motivational "attending physician" mention not linked (strict-prose). Source apostrophe variation preserved verbatim (R6 uses U+2019; R10 uses U+0027). Global counts: `planDone` 36 → 37, `planPromote` 96 (net unchanged: −ncd40.2 +cag00161N), `gem:PolicyRule` 920 → 931, `referencesPolicy` 180 → 182, `revisesPolicy` unchanged (31), `gem:ClinicalConcept` 1296 → 1298, `gem:NCDpolicy` unchanged (58; cag00161N is `gem:NCAdocument`). `policy_worklist.json`: `policies_processed` 36 → 37, `last_updated` 2026-07-03 (no new worklist entries — cag00161N minted as stub). TTL header processed-policy list → 37. SKILL.md empirical-counts marker → as of S111, 31/0/182. No SHACL / audit-script / schema change.
- **2026-07-03 / Session 112** — **Promoted NCD 10.2 (Transcutaneous Electrical Nerve Stimulation (TENS) for Acute Post-Operative Pain)** from a `planPromote` reference stub to a fully-extracted `planDone` policy in its own delimited section (`ncd10.2` was a fresh reference stub minted at S110 during NCD 160.27's extraction, not one of the 12 original TN 48 campaign stubs — 11 of those remain). 6 policy-scope rules `gemi:ncd10.2_r1`–`_r6`; 7 clinical-concept links (2 reuse + 5 mint: `conceptAcutePostOperativePain`, `conceptAcutePain`, `conceptChronicPain`, `conceptSurgery`, `conceptTENSdevice`); 3 benefit categories (reuse); 0 provider credentials; 3 policy references (cag00429N, ncd160.27, tn149 — all pre-existing stubs, no new stub minted). Reciprocal: `tn149 revisesPolicy ncd10.2` (Rev 149 produced Version 2). No new axis-2 vocabulary; 1 distinct tag (6 `gem:ruleType` triples): coverage-scope (6). Continuous-prose policy — no δ-prefix scaffold. R4 kept as one rule (Part A/Part B supply classification, not source-enumerated). "drugs" not minted (generic mass noun). No provider credential (physician mention is billing-context; strict-prose). Revision-history exclusion: TN 78; "Other Versions" Version 1 → no entity. Source apostrophe preserved verbatim (R4 uses U+2019). Global counts: `planDone` 37 → 38, `planPromote` 96 → 95 (−ncd10.2; no new stub), `gem:PolicyRule` 931 → 937, `referencesPolicy` 182 → 185, `revisesPolicy` 31 → 32, `gem:ClinicalConcept` 1298 → 1303, `gem:NCDpolicy` unchanged (58). `policy_worklist.json`: `policies_processed` 37 → 38, `last_updated` 2026-07-03 (2 new Manual-chapter Other entries `pub100_02_ch1` / `pub100_02_ch2`; NCD 10.2 appended to `pub100_02_ch15`). TTL header processed-policy list → 38. SKILL.md empirical-counts marker → as of S112, 32/0/185. No SHACL / audit-script / schema change.
- **2026-07-03 / Session 113** — **Promoted NCD 160.2 (Treatment of Motor Function Disorders with Electric Nerve Stimulation)** from a `planPromote` reference stub to a fully-extracted `planDone` policy in its own delimited section (`ncd160.2` was a fresh reference stub minted during NCD 160.7's extraction — ANNOTATION-ONLY STUBS sub-section — not a TN 48 campaign stub; 11 campaign stubs remain). 1 policy-scope rule `gemi:ncd160.2_r1`; 7 clinical-concept links (2 reuse + 5 mint: `conceptElectricNerveStimulation`, `conceptMotorFunctionDisorder`, `conceptDeepBrainStimulation`, `conceptEssentialTremor`, `conceptParkinsonsDisease`); 1 benefit category (Physicians' Services, reuse); 0 provider credentials; 3 policy references (ncd160.7 reuse; ncd160.24 + tn167 new stubs). 2 new reference stubs minted (`ncd160.24` NCD, web-resolved NCDId=279; `tn167` Coverage Issues Manual TransmittalPolicy, source URL from the PDF). Reciprocal: `tn167 revisesPolicy ncd160.2` (Rev 167 produced Version 2). No new axis-2 vocabulary; 1 distinct tag (1 `gem:ruleType` triple): non-coverage (1). **Precedent set (Tom-confirmed):** clinical concepts appearing only in a navigational "see §X" cross-reference are captured — `conceptDeepBrainStimulation` / `conceptEssentialTremor` / `conceptParkinsonsDisease` linked from the §160.24 NOTE per Option-B + presence-not-interpretation. Item/Service Description dropped as a rule (corollary #2); NOTE + Cross Reference dropped as rules (navigational, each → `referencesPolicy`); bare "the stimulator" not minted (generic). Revision-history exclusion: TN 93, CR 2553; "Other Versions" Version 1 → no entity. Source apostrophes U+0027. Global counts: `planDone` 38 → 39, `planPromote` 95 → 96 (−ncd160.2 +ncd160.24 +tn167), `gem:PolicyRule` 937 → 938, `referencesPolicy` 185 → 188, `revisesPolicy` 32 → 33, `gem:ClinicalConcept` 1303 → 1308, `gem:NCDpolicy` 58 → 59 (new ncd160.24 stub). `policy_worklist.json`: `policies_processed` 38 → 39, `last_updated` 2026-07-03 (no new worklist entries — both references minted as graph stubs; no Medicare-manual cross references). TTL header processed-policy list → 39. SKILL.md empirical-counts marker → as of S113, 33/0/188. Corrected the stale `planPromote/isInEffect=True` sub-section header (90 → accurate 94). No SHACL / audit-script / schema change.
- **2026-07-04 / Session 114** — **Promoted NCD 160.13 (Supplies Used in the Delivery of Transcutaneous Electrical Nerve Stimulation (TENS) and Neuromuscular Electrical Stimulation (NMES))** from a `planPromote` NCD stub (minted via the TN 48 §160.7.1 cross-reference at S43) to a fully-extracted `planDone` policy in its own delimited section (a TN-48-seeded reference stub, not one of the 12 original TN 48 campaign stubs — 11 of those remain unchanged). **Landed the 20-policy checkpoint (`policies_processed` = 40).** 13 policy-scope rules `gemi:ncd160.13_r1`–`_r13`; 14 clinical-concept links (6 reuse + 8 mint: `conceptFormFittingConductiveGarment`, `conceptConventionalElectrode`, `conceptAdhesiveTape`, `conceptLeadWire`, `conceptSkinProblem`, `conceptDisuseAtrophy`, `conceptRehabilitationStrengthening`, `conceptElectricalStimulation`); 1 benefit category (Incident to a physician's professional Service, reuse); 2 provider credentials (`credentialPhysician`, `credentialMedicareAdministrativeContractor`, reuse); 7 policy references (`ncd160.7`, `ncd160.12`, `ncd160.27`, `ncd280.13`, `tn78` reuse; `ncd150.4`, `ncd160.3` new stubs). 2 new reference stubs minted. Reciprocal: `tn78 revisesPolicy ncd160.13` (added alongside the pre-existing `tn78 revisesPolicy ncd160.7`; Rev 78 revised both). No new axis-2 vocabulary; 6 distinct tags (16 `gem:ruleType` triples): eligibility (9), coverage-scope (2), credentialed-actor (2), service-definition (1), documentation (1), non-coverage (1). δ-prefix scaffold across three section scopes (coverage criteria R3–R5; medical indications R6–R10; trial-period exception R12–R13); two coverage-gate umbrellas kept standalone (R2, R11). Item/Service Description kept as R1 (`service-definition`); navigational parentheticals dropped as rules (each → `referencesPolicy`). `conceptWrittenPlanOfRehabilitation` dropped (folds into R10); `conceptElectricalStimulation` minted distinct from `conceptElectricalNerveStimulation`. **URL resolution (Tom-directed):** `ncd150.4` (§150.4 → NCD §160.12) `dc:source` = the current NCD 160.12 URL (`…ncdid=175&ncdver=2&=`); `ncd160.3` (no source found) minted **without `dc:source`**. Global counts: `planDone` 39 → 40, `planPromote` 96 → 97 (−ncd160.13 +ncd150.4 +ncd160.3), `gem:PolicyRule` 938 → 951, `referencesPolicy` 188 → 195, `revisesPolicy` 33 → 34, `gem:ClinicalConcept` 1308 → 1316, `gem:NCDpolicy` 59 → 61 (two new stubs). `policy_worklist.json`: `policies_processed` 39 → 40, `last_updated` 2026-07-04 (no new worklist entries — both references minted as graph stubs; no Medicare-manual chapter cross-references). TTL header processed-policy list → 40. SKILL.md empirical-counts marker → as of S114, 34/0/195. Workflow sub-section headers: `planDone/isInEffect=True` 37 → 38, `planPromote/isInEffect=True` 94 → 95. No SHACL / audit-script / schema change. **20-policy checkpoint agenda teed up at close (not yet run).**
- **2026-07-04 / Session 115** — **Promoted NCD 160.8 (Electroencephalographic Monitoring During Surgical Procedures Involving the Cerebral Vasculature)** from a `planPromote` NCD stub — one of the 12 original TN 48 campaign stubs — to a fully-extracted `planDone` coverage policy in its own delimited section (stub removed; full block minted; workflow entry flipped `planPromote → planDone` and relocated). TN 48 campaign now **10 stubs remaining** (was 11). 4 rules `_r1`–`_r4`; 3 distinct axis-2 tags (4 `gem:ruleType` triples): eligibility (2), coverage-scope (1), credentialed-actor (1); single-domain, no domain axis, no δ-scope. 12 clinical-concept links (11 reuse from the TN 48 pass + 1 mint `conceptNeurologicalProcedure`); 3 interpretation credentials (reuse); 2 benefit categories (reuse). `referencesPolicy gemi:tn48NCD` (reciprocal `tn48 revisesPolicy ncd160.8` pre-existed). CR4278 prose-only (CR methodology pending). No new worklist entries, no new stubs. `policies_processed` 40 → 41. SKILL.md empirical-counts marker → as of S115, 34/0/196. No SHACL / audit-script / schema change. The 20-policy checkpoint (landed at S114, `policies_processed` = 40) remains teed up but not yet run.
- **2026-07-05 / Session 119** — **Promoted NCD 80.11 (Vitrectomy)** from a `planPromote` TN 48 campaign stub to a fully-extracted `planDone` policy in its own delimited section. 1 rule `gemi:ncd80.11_r1`; 1 tag: coverage-scope (1); single-domain. 8 clinical-concept links (all reuse — TN 48 pre-seeded; 0 mint); 2 benefit categories (ASC Facility Services, Physicians' Services, reuse); 0 credentials; 3 references (`tn48` reciprocal, `pub100_04_ch23`, `pub100_04_ch14`). 1 new entity minted: `gemi:pub100_04_ch14` (`gem:CMSmanualChapter`, `planPromote`). B1: five reasonable-and-necessary conditions folded into R1 (disjunctive). ASC payment/coding cross-refs → chapter `referencesPolicy` (corollary #4); CIM 35-16 prose-only. No codes (MMA §731). (Section table backfilled S120.)
- **2026-07-05 / Session 119** — **Promoted NCD 140.1 (Abortion)** from a `planPromote` TN 48 campaign stub to a fully-extracted `planDone` policy in its own delimited section. 3 rules `gemi:ncd140.1_r1`–`_r3`; 2 distinct tags (3 `gem:ruleType` triples): non-coverage (1), coverage-scope (2). 10 clinical-concept links (all reuse; 0 mint; incl. rape/incest pregnancy composites); 1 benefit category (Physicians' Services, reuse); 1 credential (`credentialPhysician`, reuse); 1 reference (`tn48` reciprocal). B: closed-gate "not covered except (1)(2)" → non-coverage base + 2 δ-scoped exceptions (NCD 160.7 R10 pattern). CIM 35-99 + statutory §§508–509 (Rev-History only) excluded. No codes (MMA §731). (Section table backfilled S120.)
- **2026-07-05 / Session 119** — **Promoted NCD 150.5 (Diathermy Treatment)** from a `planPromote` TN 48 campaign stub to a fully-extracted `planDone` policy in its own delimited section. 1 rule `gemi:ncd150.5_r1` carrying dual `gem:ruleType` (2 triples): coverage-scope (1), credentialed-actor (1). 4 clinical-concept links (all reuse; 0 mint); 2 benefit categories (Incident-to Physician's Service, Physicians' Services, reuse); 1 credential (`credentialPhysician`, reuse); 2 references (`tn48` reciprocal, `ncd240.3` Cross Reference §240.3). `credentialed-actor` from the "rendered by a physician" clause; MAC medical staff = review authority, not minted as a credential. First-sentence rationale (corollary #1) + admin note dropped. No codes. (Section table backfilled S120.)
- **2026-07-05 / Session 120** — **Promoted NCD 190.2 (Diagnostic Pap Smears)** from a `planPromote` NCD stub — one of the TN 48 campaign stubs — to a fully-extracted `planDone` policy in its own delimited section (stub metadata expanded in place; workflow entry flipped `planPromote → planDone`). TN 48 campaign now **6 stubs remaining** (was 7). 1 rule `gemi:ncd190.2_r1`; 3 distinct axis-2 tags (3 `gem:ruleType` triples): coverage-scope (1), credentialed-actor (1), eligibility (1); single-domain, no domain axis. 13 clinical-concept links (all reuse — TN 48 pre-seeded, §190.2 governing text; 0 mint), incl. both `conceptDiagnosticPapSmear` + `conceptPapSmear`; 1 benefit category (Diagnostic Laboratory Tests, reuse); 1 credential (`credentialPhysician`, reuse); 2 references (`tn48` reciprocal + `ncd210.2` Cross Reference §210.2). B1: five disjunctive conditions folded into R1 (NCD 80.11 precedent, not δ-scaffold). B2: `eligibility` retained (conditions are policy-mandated; strawman-drop rejected → SKILL.md S91 no-strawman-alternative rule codified this session). CR4278 / TN 43 revision-history prose-only, not minted; CIM 50-20 / 50-20.1 prose-only. Global counts: `referencesPolicy` 214 → 216. `policy_worklist.json`: `policies_processed` 44 → 45, `last_updated` 2026-07-05 (no new worklist entries — both references already graph stubs). TTL header processed-policy list → 45. SKILL.md empirical-counts marker → as of S120, 34/0/216. Also codified this session: SKILL.md Manual-chapter URL URI-determination rule (ch14 lesson) and the S91 no-strawman-alternative rule. No SHACL / audit-script / schema change.
- **2026-07-05 / Session 121** — **Promoted NCD 190.14 (Human Immunodeficiency Virus (HIV) Testing (Diagnosis))** from a `planPromote` TN 48-campaign stub to a fully-extracted `planDone` policy. 8 policy-scope rules `gemi:ncd190.14_r1`–`gemi:ncd190.14_r8` (coverage-scope 4, eligibility 4, frequency 2, non-coverage 1, service-standard 1; 12 `gem:ruleType` triples; no new axis-2 vocabulary). 61 `refersToClinicalConcept` links — all reuse of TN 48 §190.14 pre-seeded individuals, 0 mints; 0 benefit categories; 0 credentials. Minted 2 cited-policy stubs: `gemi:tn131NCD` (`gem:TransmittalPolicy`, current transmittal, reciprocal `revisesPolicy`) and `gemi:pub100_04_ch16` (`gem:CMSmanualChapter`, Cross Reference §120). `referencesPolicy` 216 → 218; `revisesPolicy` 34 → 35. `policies_processed` 45 → 46. R1 folds the 11 disjunctive indication findings (B1); Limitation 8 dropped as advisory (B2). TN 48 campaign now 5 stubs remaining (`ncd210.2`, `ncd230.8`, `ncd230.19`, `ncd260.1`, `ncd300.1`).
- **2026-07-05 / Session 122** — Added NCD 210.2 (Screening Pap Smears and Pelvic Examinations for Early Detection of Cervical or Vaginal Cancer) rule table: 6 rules (R1–R6), promotion of the TN 48 campaign stub `planPromote → planDone`. 8 distinct axis-2 tags used (coverage-scope, credentialed-actor, eligibility, frequency, risk-factor, documentation, service-standard, definition); no new axis-2 vocabulary. Concepts reuse-only (TN 48 §210.2 pre-seed, 44 tagged + 2 shared: `conceptPapSmear`, `conceptHIVInfection`; 0 mints). One new credential individual `gemi:credentialAuthorizedPractitioner`. Borderlines B1 (R2 separate risk-factor rule), B2 (R3 documentation capture), B3 (R6 §1861(nn) definition capture), B4 (`conceptHIVInfection` shared add), B5 (`credentialAuthorizedPractitioner` mint) — all confirmed by Tom. TN 48 campaign: 4 stubs remaining.
- **2026-07-06 / Session 123** — Added NCD 230.8 (Non-Implantable Pelvic Floor Electrical Stimulator) rule table: 2 rules `gemi:ncd230.8_r1`–`_r2`, all at policy level (no anchored coding scopes), single-domain (DME), no new axis-2 vocabulary. TN 48 campaign stub promoted `planPromote → planDone` (stub metadata expanded in place). 3 distinct axis-2 tags (4 `gem:ruleType` triples): coverage-scope (1), eligibility (2), definition (1). R1 = PFES coverage grant (stress/urge UI + cognitively-intact + failed PME trial); R2 = failed-PME-trial definition (separate rule, NCD 210.2 R6 precedent). Concepts reuse-only (19 TN 48 §230.8 pre-seed + 2 shared: `conceptUrinaryIncontinence` NCD 230.18, `conceptElectricalStimulation` NCD 160.13; 0 mints; 21 links). 0 provider credentials (agentless coverage grant). Benefit category Durable Medical Equipment (reuse). `referencesPolicy gemi:tn48NCD` (current transmittal, both directions) + `referencesPolicy gemi:cag00021N` (NCAdocument stub minted this session, URL user-supplied). Device-description paragraph dropped (corollary #2). Revision-history-only TN 133/CR 1419 + CR 4278 not minted (three-contexts / CR-methodology gate). Triple delta +14 (instances only: 2×5 base + 4 ruleType + 2 hasPolicyRule; ontology +0) plus the promoted policy block, cag00021N NCA stub, and workflow flip. No SHACL or audit-script change (no ontology-semantics change; both Plan-time gap analyses clean). `policies_processed` 47 → 48. Borderlines B1 (2 shared concepts — add), B2 (R2 separate rule), B3 (CAG-00021N mint + link) all Tom-confirmed.
- **2026-07-06 / Session 124** — Added NCD 230.19 (Levocarnitine for use in the Treatment of Carnitine Deficiency in ESRD Patients) rule table: 5 rules, all at policy level (no anchored coding scopes), single-domain (Drugs and Biologicals), no new axis-2 vocabulary. 7 distinct axis-2 tags used: coverage-scope, eligibility, definition, non-coverage, outcome-measure, documentation, credentialed-actor (11 `gem:ruleType` triples). TN 48 stub-promotion campaign: `gemi:ncd230.19` flipped `planPromote → planDone` (fourth campaign stub carrying real coverage criteria, after NCD 190.14 / 210.2 / 230.8); 2 stubs remain (`ncd260.1`, `ncd300.1`). Concepts reuse-only (21 §230.19 pre-seed + 1 shared `conceptHematocrit`; 0 mints). Policy-level credential `credentialTreatingPhysician` (reuse; continued-treatment certifier). NCA stub `gemi:cag00077N` minted (`gem:NCAdocument`, `planPromote`, URL user-supplied) and referenced. Five borderlines confirmed: B1 (conceptHematocrit shared-add), B2 (R2 definition separate), B3 (R3/R4 separate limitations), B4 (R5 documentation + credentialed-actor + policy credential), B5 (CAG-00077N mint + reference); B6 axis-2 (R3 outcome-measure tag).
- **2026-07-06 / Session 125** — **Promoted NCD 260.1 (Adult Liver Transplantation)** from a `planPromote` TN 48 campaign stub to a fully-extracted `planDone` policy. **Divergent promotion:** the live document is Version 3 (effective 2012-06-21, produced by TN 146 / CR 7908), newer than the TN-48-era stub, adding a fourth MAC-discretion coverage layer (CCA/NET/HAE) absent from the pre-seed. 5 rules `gemi:ncd260.1_r1`–`_r5`; 3 distinct axis-2 tags (7 `gem:ruleType` triples): coverage-scope (4), eligibility (2), non-coverage (1); single-domain, no δ-scope. 19 clinical-concept links (16 TN 48 §260.1 pre-seed reuse + 3 mints — CCA/NET/HAE, `dc:source gemi:tn146NCD`); 1 benefit category (Inpatient Hospital Services, reuse); 1 credential (`credentialFacilityApprovedByCMSForLiverTransplants`, reuse; policy-level). 9 new stubs minted: `tn146` (`gem:TransmittalPolicy`, current transmittal, reciprocal `revisesPolicy`), `tn2513` (`gem:TransmittalPolicy`, claims-processing, no reciprocal), `pub100_04_ch17` (`gem:CMSmanualChapter`), and 6 NCAs (`gem:NCAdocument`: CAG-00053N/00091N/00091R/00101N/00120N/00120R). `referencesPolicy` 223 → 233 (+10: tn146, tn2513, pub100_02_ch15 reuse, pub100_04_ch17, 6 NCAs); `revisesPolicy` 35 → 36 (+1: tn146). **TN 48 superseded by TN 146** — no new `referencesPolicy gemi:tn48NCD`; `tn48 revisesPolicy ncd260.1` lineage preserved (superseded-revision, Category E). CR 7908 not minted (CR-methodology gate). `policies_processed` 49 → 50. SKILL.md empirical-counts marker → as of S125, 36/0/233. Ten borderlines B1–B10 Tom-confirmed. TN 48 campaign now **1 stub remaining** (`ncd300.1`). All URLs user-supplied except tn146 (source PDF Coverage Transmittal Link). No SHACL / audit-script / schema change.
- **2026-07-06 / Session 126** — **Promoted NCD 300.1 (Obsolete or Unreliable Diagnostic Tests)** from a `planPromote` TN 48 campaign stub to a fully-extracted `planDone` policy — **the last campaign stub, closing the TN 48 stub-promotion campaign (0 remaining).** Same-era promotion (Version 2 is the TN 48 version; current Transmittal Information field = TN 48; no divergence, contrast NCD 260.1 S125). 2 policy-scope rules `gemi:ncd300.1_r1`–`_r2`; 3 distinct axis-2 tags (4 `gem:ruleType` triples): non-coverage (2), payment-rule (1), procedural (1); no new axis-2 vocabulary; no δ-scope (flat test lists folded verbatim). R1 = §300.1(A) conditional non-coverage (28 obsolete tests, payable only if the performing physician satisfactorily justifies medical need; QIO/A-B-MAC determination folded); R2 = §300.1(B) absolute non-coverage (10 outmoded phonocardiography/vectorcardiography tests). 38 clinical-concept links — **all reuse** of TN 48 §300.1 pre-seed individuals (28 subsection-A + 10 subsection-B; 0 mints). 1 benefit category (Diagnostic Tests (other), reuse); 0 credentials; 0 new stubs. `referencesPolicy gemi:tn48NCD` added (current transmittal, both directions; `tn48 revisesPolicy ncd300.1` pre-existing, preserved). TN 96 (Version 1 transmittal, Revision History only) + CR 4278 not minted. `referencesPolicy` 233 → 234; `revisesPolicy` 36 (unchanged). `policies_processed` 50 → 51. SKILL.md empirical-counts marker → as of S126, 36/0/234. Borderlines B1 (rule count 2), B2 (R1 rule-type set), B3 (0 credentials), B4 (TN 96 prose-only), B5 (no worklist Δ) all Tom-confirmed. No SHACL / audit-script / schema change.
- **2026-07-06 / Session 127** — **Extracted L33923 (Noninvasive Ear or Pulse Oximetry For Oxygen Saturation)**, a First Coast LCD, as a fresh full `planDone` policy. Added `### L33923` section (3 rules: R1/R2 coverage-scope+eligibility coverage indications, R3 coverage-scope+procedural Limitations R&N clause). 7 `gem:ruleType` triples; no new axis-2 vocabulary. 12 clinical-concept links (6 reuse + 6 mint); 3 `referencesPolicy` (A57115 stub, Pub. 100-02 Ch 15 reuse, Pub. 100-08 Ch 13 new manual-chapter stub); 0 codes / 0 credentials / no benefit category. L29370 (Sources of Information legacy LCD number) kept prose-only, not minted (Path 2, Tom-confirmed). No SHACL / audit-script / schema change.
- **2026-07-06 / Session 128** — **Promoted A57115 (Billing and Coding: Noninvasive Ear or Pulse Oximetry For Oxygen Saturation)** from a `planPromote` stub to a fully-extracted `planDone` Article. Added `### A57115` section (9 rules; 14 `gem:ruleType` triples; no new axis-2 vocabulary). Group-structured: 2 `gem:PolicyGroup` (CPT 94760/94761 → 106 ICDs; 94762 → 107 ICDs) with `exclusivelyCoversCondition`. CPT 94760/94761/94762 minted provisional in `cpt.ttl`. 4 credentials (reuse), 3 concepts (2 reuse + 1 mint), reciprocal `referencesPolicy` L33923. Borderlines (1)-(5) Tom-confirmed. No SHACL / audit-script / schema change.
- **2026-07-06 / Session 129** — **Promoted NCD 240.4 (Continuous Positive Airway Pressure (CPAP) Therapy For Obstructive Sleep Apnea (OSA))** from a `planPromote` stub to a fully-extracted `planDone` DME NCD. Added `### NCD 240.4` section (25 rules; 30 `gem:ruleType` triples; no new axis-2 vocabulary). CED clinical-study standards (8d–p) atomized into 13 `study-quality` rules per the NCD 160.18 precedent. 22 concept links (20 reuse + 2 mint), 1 credential (reuse), benefit category DME. Minted stubs: TN 96 (delivering transmittal), CAG-00093N/R/R2 (NCAs); `referencesPolicy` NCD 280.1 (cross-ref). Thresholds verbatim; A. General → concepts. Borderlines Tom-confirmed (URL-provision + no corrections = all leans). No SHACL / audit-script / schema change.
- **2026-07-15 / Session 146** — **Consolidated two duplicate mints into their `_DELETED` twins; no policy work** (`policies_processed` stays **63**). `gemi:ncd150.4` (8 triples) and `gemi:ncd160.3` (7 triples) — minted at S114 from **NCD 160.13**'s 1988-era cross-references to sections TN 48 deleted in 2006 — were deleted; NCD 160.13's two `gem:referencesPolicy` links were retargeted to `gemi:ncd150.4_DELETED` / `gemi:ncd160.3_DELETED` (reference count unchanged at 7); each bare stub's substantive finding was folded into its retiree's `gem:description` (§150.4's CIM 35-77 → §160.12 crosswalk; §160.3's TENS trial-period content) — prose only, no new graph links, conservative cross-reference principle unchanged. `planPromote/isInEffect=True` header 107 → 105. **−15 triples** (27623 → 27608), set-diff verified. No rules, concepts, credentials, benefit categories or codes touched; NCD 160.13's rule table is unaffected. The `§160.3` mention in R11 is verbatim source text and stands. New audit check `deleted_twin_collision` (YELLOW, no autofix) + V50–V52 (**49 → 52**); `SKILL.md` URI-collision pre-flight gained a third direction (retiree audit) + a failure-mode row; `gem_reference.md` §5.3 records the S114–S146 breach. Closes handoff §4 item 4 (NCD 150.4 delisted from the MCD — dissolved: the delisting is the 2006 deletion). §4 item 1: 15 → 13 NCD `planPromote` stubs.
- **2026-08-01 / Session 256** — **Extracted NCD 220.9 (Digital Subtraction Angiography; ncdid=255)** as a fresh full `planDone` policy, minted direct to the graph (never worklisted). Single-version, longstanding coverage NCD, 2 pp. **Version 1, effective 1966-01-01** (longstanding-NCD rule — V1 Tracking block declines to post the date, 01/01/1966 taken from the Other Versions column; settled S192 precedent); no implementation date (S192 drop); not a `policy_effective_date_v1` [107] candidate. 2 policy-scope rules `gemi:ncd220.9_r1`–`_r2`: R1 (`coverage-scope` + `procedural`) the MAC utilization-alertness / no-inpatient-hospitalization-solely-for-the-procedure limitation, R2 (`payment-rule`) the payment-not-to-exceed-conventional-angiography limitation; no coverage-grant rule (no explicit coverage sentence — source-fidelity / jigsaw). 4 clinical-concept links, all mints (`conceptDigitalSubtractionAngiography`, `conceptConventionalAngiography`, `conceptFluoroscopy`, `conceptRadiographicContrastMaterial`); *"vascular structures"* dropped (bare-locational, S210). 2 healthcare settings, reuse, policy-level presence (`settingOutpatient`, `settingInpatientHospital`); no rule-scoped setting-polarity edge on R1 (soft *"should not require inpatient hospitalization"* ≠ a stated denial). 0 codes / modifiers / provider credentials (the MAC is named but is not a performer and carries no delegated coverage determination — NCD 130.3 / 150.9). 1 benefit category, reuse (Diagnostic Tests (other)). **0 policy-to-policy references, 0 new stubs** — no Cross Reference, Transmittal, Revision History or NCA section in the rendition (the longstanding pre-database single-version NCD pattern, NCD 20.14 / 20.17); `source_availability_unverified` unchanged at 41. NCD census: Active, extracted 123 → 124, Total 140 → 141. `policy_worklist.json` `policies_processed` 155 → 156; TTL processed-policy header list and the `planDone / isInEffect=True` workflow-state list both → 156. `### NCD 220.9` section added to `gem_rule_categories.md`. Borderlines B1–B6, all Tom-confirmed. No ontology / SHACL / audit-script / `cpt.ttl` / `gem_llm_annotations.json` change.
- **2026-08-01 / Session 257** — **Extracted NCD 190.24 (Digoxin Therapeutic Drug Assay; ncdid=103)** as a fresh full `planDone` lab NCD, minted direct to the graph (never worklisted). Single-version, 5 pp. **Version 1, effective 2002-11-25, implementation 2003-01-01** (both posted on V1 Tracking block; not a `policy_effective_date_v1` [107] candidate). **Sixth policy of the 07/2002 lab negotiated-rulemaking batch** (after NCD 190.25 / 190.15 / 190.20 / 190.26 / 190.19): existing `gemi:tn17NCD` and `gemi:pmAB02110` each gain a reciprocal `gem:revisesPolicy gemi:ncd190.24` edge + S257 provenance note, and the policy asserts citing-side `gem:referencesPolicy` to both plus the Cross-Reference chapter `gemi:pub100_04_ch16` (B13). **3 references, all reuse — 0 new stubs.** Cross Reference *"Chapter 120"* reuses `pub100_04_ch16` — CMS's earlier form of *"Chapter 16, Section 120"*, corrected in NCD 190.20 V2 (B12). 7 policy-scope rules `gemi:ncd190.24_r1`–`_r7` (service-definition 2, coverage-scope 2, eligibility 2, frequency 2, non-coverage 1; 9 `gem:ruleType` triples; no new axis-2 vocabulary): R1 Item/Service Description, R2/R3 the two Indications clinical-indication lists, R4/R5 the frequency/steady-state guidance, R6 the digoxin therapeutic-indication background (kept, B2), R7 Limitations. 21 clinical-concept links (3 reuse — heart failure, atrial fibrillation, atrial flutter — + 18 mints, B3/B5/B6/B7/B9). Coding not in NCD narrative (points to quarterly Lab-NCD Covered Code Lists / Edit Software): 0 codes, 0 code group, 0 modifiers. 1 benefit category (Diagnostic Laboratory Tests, reuse). 0 provider credentials, 0 settings. Drops: *"digoxin therapy"* (B8), steady state + PK cluster (B10), *"acceptable cardiac diagnosis (as listed)"* (B11), standalone *"renal function"* (B4). Global counts: `revisesPolicy` 236 → 238, `referencesPolicy` 1105 → 1108. NCD census: Active, extracted 124 → 125, Total 141 → 142. `policy_worklist.json` `policies_processed` 156 → 157, `last_updated` 2026-08-01 (S257) (no new worklist entries — all references pre-existing graph individuals). TTL processed-policy header list and the `planDone / isInEffect=True` workflow-state list both → 157. `### NCD 190.24` section added to `gem_rule_categories.md`. SKILL.md empirical-counts marker → as of S257, 238/0/1108. Borderlines B1–B13, all Tom-confirmed. No ontology / SHACL / audit-script / `cpt.ttl` / `gem_llm_annotations.json` change.
- **2026-08-01 / Session 258** — **Extracted NCD 230.12 (Dimethyl Sulfoxide (DMSO); ncdid=149)** as a fresh full `planDone` drug NCD, minted direct to the graph (never worklisted). Single-version, **longstanding** coverage/non-coverage NCD, 2 pp. **Version 1, effective 1966-01-01, no implementation date** — the Tracking block declines to post V1's effective date (*"This is a longstanding national coverage determination. The effective date of this version has not been posted"*), so 1966-01-01 is read from the Other Versions column (01/01/1966 – N/A; the longstanding-NCD rule, S192 precedent, matching NCD 220.9 S256); not a `policy_effective_date_v1` [107] candidate. 4 policy-scope rules `gemi:ncd230.12_r1`–`_r4` (service-definition 1, service-standard 1, non-coverage 1, coverage-scope 1, eligibility 1; 5 `gem:ruleType` triples; no new axis-2 vocabulary): R1 Item/Service Description, R2 the FDA safe-and-effective determination, R3 all-other-indications non-coverage, R4 the interstitial-cystitis-only coverage grant. 2 clinical-concept links, **both mints** — `conceptDMSO` (prefLabel *"DMSO"*, the prose form; minted per the S48 individual-drug rule, B3) and `conceptInterstitialCystitis`. Drops: *"bladder condition"* (generic appositive descriptor; `conceptBladder` already carries *"bladder"*, B2), *"industrial solvent"* / *"wood pulp"* (non-clinical). 1 benefit category, reuse (Drugs and Biologicals). 0 codes / 0 code group / 0 modifiers / 0 provider credentials / 0 settings. **0 policy-to-policy references, 0 new stubs** — no Cross Reference, Transmittal, Revision History or NCA section in the rendition (the longstanding pre-database single-version pattern, NCD 220.9 / 20.14 / 20.17); empirical counts unchanged (238 / 0 / 1108); `source_availability_unverified` unchanged at 41. NCD census: Active, extracted 125 → 126, Total 142 → 143. `policy_worklist.json` `policies_processed` 157 → 158, `last_updated` 2026-08-01 (S258) (no new worklist entries — 0 references). TTL processed-policy header list and the `planDone / isInEffect=True` workflow-state list both → 158. `### NCD 230.12` section added to `gem_rule_categories.md`. SKILL.md empirical-counts marker → as of S258, 238/0/1108. Borderlines B1–B3, all Tom-confirmed. No ontology / SHACL / audit-script / `cpt.ttl` / `gem_llm_annotations.json` change.
- **2026-08-01 / Session 259** — **Extracted NCD 20.24 (Displacement Cardiography; ncdid=262)** as a fresh full `planDone` diagnostic-test NCD, minted direct to the graph (never worklisted). Single-version CIM-era NCD, 2 pp. **Version 1, effective 1988-10-12** (posted directly in the Tracking block; a normal single-version NCD, not a longstanding-NCD Other-Versions read; not a `policy_effective_date_v1` [107] candidate). 4 policy-scope rules `gemi:ncd20.24_r1`–`_r4` (service-definition 1, coverage-scope 2, eligibility 1, non-coverage 1; 5 `gem:ruleType` triples; no new axis-2 vocabulary): R1 Item/Service Description (captured whole — no HCPCS code carries the service identity, the NCD 110.22 R1 shape), R2 the cardiokymography date-of-service coverage boundary, R3 the cardiokymography coverage gate (adjunct-to-ECG-stress-testing + male/female clinical indications folded verbatim), R4 photokymography excluded. The R2/R3 split (rather than one folded Pattern-3 rule) is **B1**. 8 clinical-concept links, **6 mints + 2 reuse**: mints `conceptDisplacementCardiography`, `conceptCardiokymography`, `conceptPhotokymography` (non-covered, still linked — presence, not polarity), `conceptElectrocardiographicStressTesting`, `conceptAtypicalAnginaPectoris`, `conceptNonischemicChestPain`; reuse `conceptCoronaryArteryDisease` and `conceptAngina` (**B2** — the female indication reuses the existing `gemi:conceptAngina`, prefLabel *"angina"*, minted S196 at NCD 190.15, on exact-prefLabel match rather than reusing `conceptAnginaPectoris` or minting a duplicate; the duplicate mint was caught by the audit's `uri_collision` check pre-close; *"either typical or atypical"* kept verbatim, not split). 1 benefit category, reuse (Diagnostic Tests (other)). 0 codes / 0 modifiers / 0 code group / 0 provider credentials / 0 settings. **1 policy-to-policy reference, 1 new stub** — TN 33 → `gemi:tn33CIM` (**B3**): era gate makes it a CIM (HCFA Pub. 6) coverage transmittal (09/1988 predates the NCD Manual; corroborated by `tn29CIM`/`tn34CIM`/`tn36CIM` in the CIM's monotonic dated series); named in the Transmittal Information field and the 09/1988 Revision History entry describing the change it made, so it takes `gem:revisesPolicy gemi:ncd20.24` (S151 transmittal-edge rule) and the policy asserts the reciprocal `gem:referencesPolicy`. No CR (predates the CR system); source URL pending (pre-2000 CIM), `gem:sourceAvailability` not asserted → `source_availability_unverified` 41 → 42. Global counts: `revisesPolicy` 238 → 239, `referencesPolicy` 1108 → 1109. NCD census: Active, extracted 126 → 127, Total 143 → 144. `policy_worklist.json` `policies_processed` 158 → 159, `last_updated` 2026-08-01 (S259) (no new worklist entries — the reference is minted direct to the graph as a stub). TTL processed-policy header list, the `planDone / isInEffect=True` workflow-state list (158 → 159), and the `planPromote / isInEffect=True` list (705 → 706) updated. `### NCD 20.24` section added to `gem_rule_categories.md`. SKILL.md empirical-counts marker → as of S259, 239/0/1109. Borderlines B1–B3, all Tom-confirmed. No ontology / SHACL / audit-script / `cpt.ttl` / `gem_llm_annotations.json` change.
- **2026-08-04 / Session 260** — Added **NCD 130.4** (Electrical Aversion Therapy for Treatment of Alcoholism; ncdid=31) as a fresh extraction: **2 rules**, +77 triples. Single-version longstanding non-coverage NCD, minted direct to the graph (never worklisted). Effective date 1966-01-01 read from the Other Versions table under the longstanding-NCD rule (Tracking Information declines to post it); no implementation date published or asserted; V1 in hand, so not a `[107]` candidate. 0 codes / 0 modifiers / 0 credentials / 0 settings / 0 code groups — a pure prose non-coverage policy. 9 clinical-concept links (6 mints, 3 reuse); 3 `referencesPolicy` links with **0 new stubs**, all three targets already `planDone`. No new axis-2 vocabulary; 2 tags used: service-definition, non-coverage. Borderlines B1–B5 all Tom-confirmed: B1 Item/Service Description as one Pattern-3 `service-definition` rule (no HCPCS code carries the service identity); B2 `conceptElectricStimulation` minted against the existing `conceptElectricalStimulation` (the electric/electrical precedent is already in the corpus); B3 `conceptAbstinenceFromIngestionOfAlcoholicBeverages` minted as a composite rather than reusing `conceptAlcoholAbstinence`; B4 "taste, smell and sight" dropped as generic sensory modalities; B5 the Cross Reference sentence yields no rule but does yield the three links (presence, not governance). `policies_processed` 159 → 160, which brings the 20-policy cadence checkpoint **due**. First extraction to read its source from the committed `sources/` library (`sources/NCD 130.4.pdf`, text-layer, ungated).
